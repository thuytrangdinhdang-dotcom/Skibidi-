local loaded = loadstring(game:HttpGet("https://raw.githubusercontent.com/thuytrangdinhdang-dotcom/Skibidi-/refs/heads/main/Uiiii", true))()
local makeWindow = loaded:MakeWindow({
    SaveFolder = 'VxzScriptPremium.json',
    SubTitle = 'By Vxz X Will Team',
    Title = 'Vxz Hub [Beta] : Steal An Egg | Trộm một quả trứng'
})
local newMinimizer = makeWindow:NewMinimizer({
    KeyCode = Enum.KeyCode.LeftControl
})
local createMobileMinimizer = newMinimizer:CreateMobileMinimizer({
    Image = 'rbxassetid://127729348105692',
    BackgroundTransparency = 1,
    Size = UDim2.new(ClickState, 55, ClickState, 55)
})
local typeofResult22 = typeof(createMobileMinimizer)
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(ClickState, 18)
UICorner.Parent = createMobileMinimizer.Instance
createMobileMinimizer.Instance.Size = UDim2.new(ClickState, 55, ClickState, 55)
createMobileMinimizer.Instance.BackgroundTransparency = 1
local makeTab = makeWindow:MakeTab({
    Title = 'Thông Tin',
    Icon = 'Info'
})
